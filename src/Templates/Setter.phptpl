
    /**
     * @param {{ type }} ${{ attributeName }}
     *
     * @return $this
     */
    public function set{{ functionName }}({{ type }} ${{ attributeName }}): {{ className }}
    {
        $this->{{ attributeName }} = ${{ attributeName }};
        return $this;
    }
