{
  "$id": "Person",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "enum": ["Lawrence", "Hans"],
      "description": "The name of the person",
      "default": "Hans",
      "example": "Lawrence"
    },
    "age": {
      "type": "integer",
      "description": "The age of the person",
      "example": 42
    }
  },
  "required": [
    "name"
  ]
}