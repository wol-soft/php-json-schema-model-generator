{
  "type": "object",
  "properties": {
    "property1": {
      "$id": %s,
      "enum": %s,
      "enum-map": %s
    },
    "property2": {
      "$id": %s,
      "enum": %s,
      "enum-map": %s
    }
  }
}